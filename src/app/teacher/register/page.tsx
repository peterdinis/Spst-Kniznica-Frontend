import TeacherRegisterForm from '@/app/components/auth/TeacherRegisterForm';
import { NextPage } from 'next';

const TeacherRegisterPage: NextPage = () => {
    return <TeacherRegisterForm />;
};

export default TeacherRegisterPage;
